{% extends 'base.html.twig' %}

{% block title %}Animal{% endblock %}

{% block body %}
    <h1>Animal</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ animal.id }}</td>
            </tr>
            <tr>
                <th>Codigo</th>
                <td>{{ animal.codigo }}</td>
            </tr>
            <tr>
                <th>Leite</th>
                <td>{{ animal.leite }}</td>
            </tr>
            <tr>
                <th>Racao</th>
                <td>{{ animal.racao }}</td>
            </tr>
            <tr>
                <th>Peso</th>
                <td>{{ animal.peso }}</td>
            </tr>
            <tr>
                <th>Nascimento</th>
                <td>{{ animal.nascimento ? animal.nascimento|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_animal_index') }}">back to list</a>

    <a href="{{ path('app_animal_edit', {'id': animal.id}) }}">edit</a>

    {{ include('animal/_delete_form.html.twig') }}
{% endblock %}
