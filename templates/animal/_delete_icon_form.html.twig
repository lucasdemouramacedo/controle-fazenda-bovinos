<form method="post" class="" style="display: contents;width: 40px; height: 40px; margin:0 !important" action="{{ path('app_animal_delete', {'id': animal.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ animal.id) }}">
    <button class="btn btn-danger btn-sm px-2 py-2 mx-1" style="width: 40px; height: 40px"><i class="fa-solid fa-trash-can"></i></button>
</form>
