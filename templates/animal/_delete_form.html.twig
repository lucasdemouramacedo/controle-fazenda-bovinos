<div class="col-12 col-sm-4 col-md-3 col-lg-2 order-1 order-sm-1 order-md-1">
    <form method="post"class="col-12 p-0" style="margin:0 !important" action="{{ path('app_animal_delete', {'id': animal.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ animal.id) }}">
        <button class="btn btn-danger col-12">Apagar</button>
    </form>
</div>